{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@upstash/context7-mcp",
        "--api-key",
        {{ (bitwarden "item" "Context7 MCP").login.password | trim | quote }}
      ],
      "enabled": true
    }
  }
}
