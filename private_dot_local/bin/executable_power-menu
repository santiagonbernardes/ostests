#!/usr/bin/env bash

option=$(echo -e "Lock\nReboot\nShutdown" | rofi -dmenu -p "" -format i)

case "$option" in
0)
  hyprlock
  ;;
1)
  hyprshutdown -t 'Restarting...' --post-cmd 'reboot'
  ;;
2)
  hyprshutdown -t 'Shutting down...' --post-cmd 'shutdown -P 0'
  ;;
esac
